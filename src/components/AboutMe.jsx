import React from 'react';
import {starWarsInfo} from "../utils/constants.js";

const AboutMe = () => {
    return (
        <div>
            {starWarsInfo}
        </div>
    );
};

export default AboutMe;